# 基于移动端拍卖系统 — 项目文档

---

## 一、项目结构

```
web课设/
├── server/                          # Node.js 后端
│   ├── src/
│   │   ├── index.ts                 # 入口: Express + WebSocket + 定时器
│   │   ├── config/
│   │   │   └── env.ts               # 环境变量校验 (zod)
│   │   ├── db/
│   │   │   ├── prisma.ts            # Prisma Client (adapter-pg)
│   │   │   ├── redis.ts             # ioredis 连接
│   │   │   └── pg.ts                # 原生 pg 连接
│   │   ├── ai/
│   │   │   └── client.ts            # OpenAI 兼容 Chat + Embedding 客户端
│   │   ├── middleware/
│   │   │   ├── auth.ts              # JWT 鉴权
│   │   │   ├── role.ts              # 角色守卫
│   │   │   ├── error.ts             # 全局错误处理
│   │   │   ├── upload.ts            # 图片上传 (multer)
│   │   │   └── audit.ts             # 操作日志中间件
│   │   ├── modules/
│   │   │   ├── user/                # 注册/登录 /me
│   │   │   ├── item/                # 拍品 CRUD + 审核
│   │   │   ├── auction/             # 拍卖场次 + 定时开拍/结拍
│   │   │   ├── bid/                 # 出价 HTTP + 落库
│   │   │   ├── order/               # 订单 支付/发货/确认 + 成交向量入库
│   │   │   ├── dispute/             # 争议 发起/受理/解决
│   │   │   ├── ai/                  # AI 描述优化 + RAG 估价
│   │   │   └── admin/               # 后台: 看板/用户管理/日志/配置
│   │   ├── utils/
│   │   │   ├── jwt.ts               # JWT 签名/验签
│   │   │   ├── password.ts          # bcrypt 加密
│   │   │   ├── response.ts          # 统一响应格式
│   │   │   └── error.ts             # AppError 类
│   │   └── ws/
│   │       ├── index.ts             # WebSocket 发布订阅 + 竞价
│   │       └── scheduler.ts         # 定时器(10s): 自动开拍/结拍
│   ├── prisma/
│   │   ├── schema.prisma            # 12 张数据表 + pgvector
│   │   └── seed.ts                  # 4 角色种子账号
│   ├── prisma.config.ts             # Prisma 7 配置文件
│   └── uploads/                     # 图片上传存储目录
│
├── admin/                           # Vue3 后台管理
│   ├── src/
│   │   ├── main.ts                  # 入口
│   │   ├── App.vue
│   │   ├── router/
│   │   │   └── index.ts             # 路由 + 鉴权守卫
│   │   ├── stores/
│   │   │   └── auth.ts              # Pinia 登录状态
│   │   ├── api/
│   │   │   ├── auth.ts              # 登录 API
│   │   │   ├── admin.ts             # 管理端 API
│   │   │   └── item.ts              # 拍品 API
│   │   ├── layouts/
│   │   │   └── AdminLayout.vue      # 侧边栏布局
│   │   └── views/
│   │       ├── Login.vue            # 登录页
│   │       ├── Dashboard.vue        # 数据看板(6 个统计卡片)
│   │       ├── Users.vue            # 用户管理(CRUD+搜索+分页)
│   │       ├── Items.vue            # 拍品审核(通过/驳回)
│   │       ├── Auctions.vue         # 拍卖场次列表
│   │       ├── Disputes.vue         # 争议处理(受理/解决/驳回)
│   │       ├── Logs.vue             # 操作日志审计
│   │       └── Config.vue           # 系统配置
│   └── vite.config.ts
│
├── miniprogram/                     # 微信小程序
│   ├── app.js / app.json / app.wxss
│   ├── project.config.json
│   ├── utils/api.js                 # HTTP 请求封装 (Promise + 鉴权)
│   └── pages/
│       ├── index/                   # 首页 - 拍卖大厅列表
│       ├── auction/                 # 拍卖详情
│       ├── bid/                     # WebSocket 实时竞价
│       ├── publish/                 # 发布拍品 + AI 优化 + 图片上传
│       ├── login/                   # 注册即登录(用户名已存在自动登录)
│       ├── mine/                    # 个人中心(入口: 拍品/订单/估价/争议)
│       ├── myItems/                 # 我的拍品(已审核可开启拍卖)
│       ├── orders/                  # 我的订单(支付/确认收货)
│       ├── valuation/               # AI 估价助手(多轮对话)
│       └── dispute/                 # 发起争议
│
├── docker-compose.yml               # PostgreSQL(pgvector) + Redis
├── package.json                     # Monorepo 根配置
├── pnpm-workspace.yaml
└── .gitignore
```

---

## 二、期望实现的功能

### 2.1 系统基本功能

| 编号 | 功能 | 描述 |
|------|------|------|
| F-01 | 用户注册登录 | 卖家/买家注册,后台管理员预设账号登录 |
| F-02 | 四角色权限 | 卖家/买家/拍卖管理员/系统管理员,各有标准操作 |
| F-03 | 拍品 CRUD | 卖家创建/编辑/删除拍品,含图片上传(单次最多 5 张) |
| F-04 | 拍品审核 | 拍卖管理员审核拍品(通过/驳回),驳回可附原因 |
| F-05 | 拍卖场次 | 卖家对审核通过拍品开启拍卖,设定结束时间 |
| F-06 | 定时自动开拍 | 到达开始时间自动将 scheduled→live |
| F-07 | 实时竞价 | WebSocket 订阅 + HTTP 双重出价通道,全播广播 |
| F-08 | 定时结拍 | 到达结束时间自动结算中标者,生成订单 |
| F-09 | 订单支付 | 买家模拟支付,状态流转: pending→paid |
| F-10 | 卖家发货 | 卖家标记已发货: paid→shipped |
| F-11 | 确认收货 | 买家确认: shipped→completed |
| F-12 | 订单取消 | 买家/管理员可取消未支付或已支付订单 |
| F-13 | 争议处理 | 买家/卖家发起争议 → 拍卖管理员受理/解决/驳回 |
| F-14 | 用户管理 | 系统管理员 CRUD 用户,启用/禁用 |
| F-15 | 系统配置 | 系统管理员读写配置项 |
| F-16 | 操作日志 | 自动记录 9 类关键操作,支持审计查询 |
| F-17 | 数据看板 | 后台首页 6 个统计卡片 |

### 2.2 AI 能力考核任务

| 编号 | 功能 | 等级 | 描述 |
|------|------|------|------|
| F-18 | AI 拍品描述优化 | ☆ 必选 | 卖家输入名称+成色+原价→后端调用大模型生成卖点文案→卖家可编辑后发布 |
| F-19 | AI 智能估价助手 | ★ 加分 | 成交记录 bge-m3 向量化→pgvector 存储→估价时向量检索同类+大模型生成建议区间与策略→支持多轮对话 |

---

## 三、技术栈

| 层 | 技术 |
|------|------|
| 移动端 | 微信小程序原生 (WXML/WXSS/JS) |
| 后台前端 | Vue3 + Vite + Element Plus + Pinia + vue-router |
| 后端 | Node.js + Express + TypeScript |
| 数据库 ORM | Prisma 7 + @prisma/adapter-pg |
| 业务库 | PostgreSQL 15 |
| 向量库 | pgvector 0.8.4 扩展 (同一 PG 实例) |
| 缓存 | Redis 7 (竞价排行/倒计时/在线) |
| 实时通信 | WebSocket (ws 库) |
| 大模型 Chat | opencode /zen/go/v1 (mimo-v2.5 / deepseek-v4-flash) |
| 大模型 Embedding | 硅基流动 BAAI/bge-m3 (1024 维) |
| 鉴权 | JWT (jsonwebtoken) |
| 密码 | bcryptjs |
| 参数校验 | zod |
| 容器化 | Docker Compose |

---

## 四、遇到的 Bug 与解决方案

### 4.1 已解决的 Bug

| 编号 | Bug 描述 | 原因 | 解决方案 |
|------|----------|------|----------|
| B-01 | ESPERM 安装依赖失败 `esbuild` | Windows 上 esbuild.exe 被 Defender 锁定,pnpm rename 操作失败 | 改用 npm 安装,配置 `.npmrc` 用 hoisted 模式 |
| B-02 | Prisma 7 不支持 `datasource.url` | Prisma 7 去掉了 schema 中 datasource url,需要 driver adapter | 加 `prisma.config.ts` 配置,用 `@prisma/adapter-pg` |
| B-03 | PrismaClient 需传 adapter 构造 | Prisma 7 engine type=client 强制要求 adapter 或 accelerateUrl | 改用 `PrismaPg` adapter + `Pool` |
| B-04 | pgvector `registerTypes` 不可用 | pgvector npm 包 API 与文档不一致 | 改用原生 SQL 传 `$x::vector` 显式转换 |
| B-05 | 微信小程序 `wx.request` 400 JSON 解析失败 | `wx.request` 默认 form-encoded,Express 只解析 JSON | 用封装好的 `utils/api.js` 设置 `Content-Type: application/json` |
| B-06 | Windows 上 curl.exe JSON 转义问题 | PowerShell 中 `'{\\"key\\":\\"val\\"}'` 被额外转义 | 改用 `WriteAllText` 写入文件 + `--data-binary @file` |
| B-07 | `DataCloneError` XMLHttpRequest JSON | 请求体包含非 UTF-8 字符 | 用 `System.Text.UTF8Encoding($false)` 无 BOM 写入 |
| B-08 | 拍卖场次"请求失败(400)" | 拍品已存在拍卖场次,按钮仍显示"开启拍卖" | `listMine`加 `include: { auction }`,前端判断 `item.auction` 是否存在 |
| B-09 | 订单 403"非本人订单" | 卖家登录后在"我的订单"中看到买家订单并点支付 | 添加 `myUserId` 比对,仅买家显示支付按钮 |
| B-10 | 微信开发者工具 400 "用户名已被占用" | 输入框中文字符编码错误 | 改用 `utils/api.js` 封装的方法(post),确保 Content-Type |
| B-11 | AI 文案生成后 textarea UI 错乱 | 微信原生 textarea 溢出覆盖下方按钮 | `auto-height` + `max-height: 400rpx` + `overflow-y: auto` |
| B-12 | `selectedColor` 缺字符 | app.json 中 `"#1296db"` 写成了 `"#296db"` | 修正为 `"#1296db"` |
| B-13 | 小程序 `require` 模块错误时白屏 | `api.js` 使用了 ES6 语法(默认参数/箭头函数/解构)但增强编译未正确处理 | 全部改写为 ES5 兼容语法(`var`/`function`) |
| B-14 | 估价助手看不到用户消息 | `scroll-view` 需要固定高度,flex:1 在模拟器中不工作 | 换成普通 `view` + `overflow-y: auto` + flex 布局 |
| B-15 | Prisma 7 迁移 `SoldItemEmbedding` 无法识别 `Unsupported("vector(1024)")` | Prisma migrate 会建立表,但 `Unsupported` 字段类型仅 SQL 中手动定义 | migrate 自动创建表结构,vector 类型由 pgvector 镜像自带 |
| B-21 | `GET /api/bids/auction/:auctionId` 需登录 | `index.ts` 将 `/api/bids` 整组挂载了 `auth` 中间件 | 移除路由级 `auth`,各子路由各自控制 |
| B-22 | `DELETE /items/:id` 无拍卖检查 | 拍卖进行中的拍品可被软删除,导致场次悬空 | 新增 `auction.findFirst` 查询 `scheduled/live` 状态,存在则拒绝删除 |
| B-23 | `forceEnd()` 未写入成交向量 | `SoldItemEmbedding` 仅在 `pay()` 中索引 | `forceEnd()` 交易后调用 `indexSoldItem()` 写入 |
| B-24 | `endExpiredAuctions()` 未写入成交向量 | 同上,自动结拍不触发向量索引 | 创建订单后调用 `indexSoldItem()` |
| B-25 | `POST /admin/auctions/:id/force-cancel` 缺少 `reason` 校验信息 | 无单独文档,测试时直接返回 422 | 已在 admin 路由中明确 `reasonSchema` (zod: min1 max200) |

### 4.2 尚未修复的 Bug / 待改进

| 编号 | 问题 | 优先级 | 说明 |
|------|------|--------|------|
| B-16 | 定时器重复启动 | 低 | 多个 node 进程同时运行会导致 scheduler 重复,需进程管理 |
| B-17 | 图片上传未限制文件大小 | 中 | `multer` 设了 5MB 限制但微信端未校验 |
| B-19 | 小程序端 WebSocket 断线重连 | 低 | bid 页 ws 断开后不自动重连 |
| B-20 | admin 前端配置页没有种子数据 | 低 | system_config 表为空,需要先通过 API 插入配置项 |

---

## 五、测试账号

| 角色 | 账号 | 密码 | 入口 |
|------|------|------|------|
| 卖家 | `seller01` | `123456` | 微信小程序 |
| 买家 | `buyer01` | `123456` | 微信小程序 |
| 拍卖管理员 | `auction01` | `123456` | 后台 `http://localhost:5173` |
| 系统管理员 | `admin01` | `123456` | 后台 `http://localhost:5173` |

---

## 六、API 接口总览

| 模块 | 方法 | 路径 | 鉴权 | 说明 |
|------|------|------|------|------|
| User | POST | `/api/users/register` | - | 注册(buyer/seller) |
| User | POST | `/api/users/login` | - | 登录返回 JWT |
| User | GET | `/api/users/me` | JWT | 当前用户信息 |
| Item | GET | `/api/items` | - | 拍品列表(支持分页/筛选/搜索) |
| Item | GET | `/api/items/mine` | JWT | 我的拍品(含 auction 状态) |
| Item | GET | `/api/items/:id` | - | 拍品详情 |
| Item | POST | `/api/items` | JWT seller | 创建拍品 |
| Item | PUT | `/api/items/:id` | JWT seller | 修改拍品 |
| Item | DELETE | `/api/items/:id` | JWT | 删除(status→removed,有进行中拍卖则拒绝) |
| Item | POST | `/api/items/:id/review` | JWT admin | 审核(通过/驳回) |
| Item | POST | `/api/items/upload` | JWT seller | 上传图片 |
| Auction | GET | `/api/auctions` | - | 拍卖场次列表 |
| Auction | GET | `/api/auctions/:id` | - | 详情(含前 10 出价) |
| Auction | POST | `/api/auctions` | JWT seller | 创建拍卖场次 |
| Auction | DELETE | `/api/auctions/:id` | JWT | 取消 |
| Bid | POST | `/api/bids` | JWT | 出价 |
| Bid | GET | `/api/bids/mine` | JWT | 我的出价记录 |
| Bid | GET | `/api/bids/auction/:aid` | - | 场次出价排行 |
| Order | GET | `/api/orders` | JWT | 订单列表 |
| Order | GET | `/api/orders/:id` | JWT | 订单详情 |
| Order | POST | `/api/orders/:id/pay` | JWT buyer | 支付 |
| Order | POST | `/api/orders/:id/ship` | JWT seller | 发货 |
| Order | POST | `/api/orders/:id/complete` | JWT buyer | 确认收货 |
| Order | POST | `/api/orders/:id/cancel` | JWT | 取消订单 |
| AI | POST | `/api/ai/optimize-description` | JWT | **AI 描述优化** |
| AI | POST | `/api/ai/valuation` | JWT | **AI 估价助手(RAG+多轮)** |
| Dispute | POST | `/api/disputes` | JWT buyer/seller | 发起争议 |
| Dispute | GET | `/api/disputes` | JWT | 争议列表(用户看自己) |
| Dispute | GET | `/api/disputes/:id` | JWT | 争议详情 |
| Dispute | POST | `/api/disputes/:id/take` | JWT admin | 受理 |
| Dispute | POST | `/api/disputes/:id/resolve` | JWT admin | 解决/驳回 |
| Admin | GET | `/api/admin/stats` | JWT admin | 数据看板 |
| Admin | GET | `/api/admin/users` | JWT sys | 用户管理列表 |
| Admin | POST | `/api/admin/users` | JWT sys | 创建用户 |
| Admin | PUT | `/api/admin/users/:id` | JWT sys | 编辑用户 |
| Admin | DELETE | `/api/admin/users/:id` | JWT sys | 禁用用户 |
| Admin | GET | `/api/admin/items/pending` | JWT admin | 待审核拍品 |
| Admin | GET | `/api/admin/auctions` | JWT admin | 拍卖列表 |
| Admin | POST | `/api/admin/auctions/:id/force-end` | JWT admin | 强制结束 |
| Admin | POST | `/api/admin/auctions/:id/force-cancel` | JWT admin | 强制取消 |
| Admin | GET | `/api/admin/logs` | JWT sys | 操作日志 |
| Admin | GET | `/api/admin/config` | JWT sys | 配置列表 |
| Admin | PUT | `/api/admin/config/:key` | JWT sys | 更新配置 |
| Health | GET | `/health` | - | 健康检查 |

---

## 七、数据库表结构 (PostgreSQL + pgvector)

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| User | 用户 | username, password, role(enum), status |
| Item | 拍品 | sellerId, name, condition, originPrice, startPrice, status(enum) |
| Auction | 拍卖场次 | itemId(unique), sellerId, type, status, startPrice, currentPrice, minIncrement, startTime, endTime, winnerId |
| Bid | 出价记录 | auctionId, userId, amount, status(enum), isMax |
| Order | 订单 | auctionId(unique), buyerId, finalPrice, status(enum) |
| Dispute | 争议 | auctionId, openedBy, handledBy, reason, status, resolution |
| SoldItemEmbedding | 成交向量 | itemId, name, category, condition, finalPrice, **embedding(vector(1024))** |
| AiValuationSession | 估价会话 | userId, itemId, title, messages(jsonb), recommendations(jsonb) |
| AiDescriptionHistory | 优化历史 | userId, itemId, rawInput(jsonb), output(text), model |
| Session | 登录会话 | userId, expiresAt |
| SystemConfig | 系统配置 | key(unique), value, description |
| OperationLog | 操作日志 | userId, action, target, detail(jsonb) |

---

## 八、启动方式

```powershell
# 1. 启动数据库容器
cd C:\Users\34260\Desktop\web课设
docker compose up -d

# 2. 启动后端 (终端 1)
cd server
npx tsx src\index.ts

# 3. 启动后台前端 (终端 2)
cd admin
npm run dev

# 4. 微信开发者工具
# 导入 miniprogram 目录 → 测试号 → 不校验合法域名
```

---

## 九、技术要点记录

### 9.1 AI 描述优化流程
```
卖家输入[名称+成色+原价] → POST /api/ai/optimize-description
 → opencode /zen/go/v1 (mimo-v2.5) → 生成 200-400 字文案
 → 存入 AiDescriptionHistory 表 → 返回给前端 textarea
 → 卖家可编辑后提交拍品
```

### 9.2 RAG 估价助手流程
```
① 历史成交向量化(3个触发时机):
  买家支付订单 → order.pay()  → indexSoldItem()
  管理员强制结拍 → forceEnd() → indexSoldItem()
  定时器自动结拍 → endExpiredAuctions() → indexSoldItem()
  → bge-m3(硅基流动) → vector(1024) → INSERT INTO SoldItemEmbedding

② 估价查询:
  卖家提问 → POST /api/ai/valuation
  → bge-m3 向量化查询 → pgvector cosine 检索(<=>) → 同类 TOP5
  → 拼接检索结果到 prompt → mimo-v2.5 生成估价建议
  → sessionId 支持多轮追问
```

### 9.3 WebSocket 竞价
```
客户端连接: ws://localhost:3000/ws?token=<jwt>
订阅: {"action":"subscribe","auctionId":1}
出价: {"action":"bid","amount":100}
服务端: bidService.placeBid() → 落库 → broadcastAll(在场所有客户端)
```

### 9.4 定时调度
```
Scheduler 每 10 秒:
  ① startScheduledAuctions(): scheduled + startTime<=now → live
  ② endExpiredAuctions():
     live + endTime<=now
       → 有出价: settled + 创建order + indexSoldItem()写入向量
       → 无出价: ended
```

---

## 十、接口测试覆盖情况

以下为全量测试结果（2026-07-05 验证）：

| 测试项 | 结果 | 备注 |
|--------|------|------|
| Health check | ✅ | `GET /health` 返回 `{"ok":true}` |
| 注册 + 四角色登录 | ✅ | seller/buyer/auction_admin/system_admin |
| 拍品 CRUD | ✅ | 含 `submitForReview:true` 提审 |
| 图片上传 | ✅ | multer 单文件上传,返回 URL |
| 拍品审核 | ✅ | `{"approved":true}` 通过, `false`+reason 驳回 |
| 创建拍卖(type=ascending) | ✅ | 枚举校验: ascending/sealed/descending |
| 卖家取消拍卖(scheduled) | ✅ | `DELETE /api/auctions/:id` |
| 自动开拍(scheduled→live) | ✅ | scheduler 每 10s |
| 出价(HTTP+WebSocket) | ✅ | 双重通道,WS 全播 broadcast |
| 出价排行(公开) | ✅ | `GET /api/bids/auction/:aid` 无 auth |
| 自动结拍→订单 | ✅ | live→settled(有出价)/ended(无出价)+向量写入 |
| 强制结束→订单 | ✅ | force-end + indexSoldItem() |
| 强制取消 | ✅ | 需传 `reason` 字段 |
| 订单生命周期 | ✅ | pay→ship→complete 均 200 |
| 争议生命周期 | ✅ | create→take→resolve |
| AI 描述优化 | ✅ | mimo-v2.5 生成文案 |
| AI RAG 估价 | ✅ | 向量检索 + 多轮对话 |
| 管理端数据看板 | ✅ | 6 项统计 |
| 管理端用户 CRUD | ✅ | create/update/disable |
| 管理端日志 | ✅ | 操作日志审计 |
| 管理端配置 | ✅ | GET+PUT |
| 角色权限边界 | ✅ | buyer 不可创建拍卖, seller 不可出价/审核 |
| 删除有拍卖的拍品 | ✅ | 阻塞: "有进行中的拍卖场次" |

---

## 十一、环境依赖

| 工具 | 版本要求 | 用途 |
|------|----------|------|
| Node.js | ≥ 20 | 运行环境 |
| npm | ≥ 10 | 包管理 |
| Docker Desktop | ≥ 4.80 | PostgreSQL + Redis 容器 |
| 微信开发者工具 | Stable | 小程序调试 |
| PostgreSQL 客户端 | 可选 | 手动连数据库 |
| Redis CLI | 可选 | 手动连 Redis |
